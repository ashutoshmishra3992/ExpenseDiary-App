/**
 * @format
 */

import { AppRegistry } from 'react-native';
import { name as appName } from './app.json';
import ExpenseDiaryApp from './src/ExpenseDiaryApp';

AppRegistry.registerComponent(appName, () => ExpenseDiaryApp);
